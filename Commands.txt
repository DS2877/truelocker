För egen del

Mall för att skapa frontend(bash):
npx create-react-app .


Kör blockkedjan lokalt(bash):
cd backend
anvil

testning t.ex:
forge test --fork-url http://127.0.0.1:8545

Koppling front och smart kontrakt:
npm install ethers
npm install @web3modal/ethereum @web3modal/react --legacy-peer-deps

npm install axios --legacy-peer-deps
npm install crypto-js

Styling:
npm install -D tailwindcss
npx tailwindcss init -p
npx --ignore-existing tailwindcss init -p

För Pinata API:
npm install dotenv

forge build

.env 
echo $PRIVATE_KEY
echo $RPC_URL

cast block-number --rpc-url $RPC_URL
forge script script/Deploy.s.sol --rpc-url $RPC_URL --private-key $PRIVATE_KEY --broadcast

export $(grep -v '^#' .env | xargs)
forge script script/Deploy.s.sol \
  --rpc-url $RPC_URL \
  --private-key $PRIVATE_KEY \
  --broadcast

  https://faucets.chain.link/sepolia


Start: 
bash
cd backend
anvil

bash
$ forge script script/Deploy.s.sol \
  --rpc-url http://127.0.0.1:8545 \
  --broadcast \
  --sender (Från ANVIL)

$ forge script script/Deploy.s.sol \
  --rpc-url http://127.0.0.1:8545 \
  --broadcast \
  --private-key 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 (från anvil)